- defaults:
    link:
      create: true
      relink: true
      force: true

- link:
    ~/.vim: vim
    ~/.vimrc: vim/vimrc
    ~/.ghc/ghci.conf: haskell/ghci.conf
    ~/.fonts: fonts
    ~/.gitconfig: git/gitconfig
    ~/.githelpers: git/githelpers
    ~/.config/fish/omf: fish/omf

- clean: ['~']
- clean: [~/.config]

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [vim +PlugInstall, Installing Vim Plugins]

